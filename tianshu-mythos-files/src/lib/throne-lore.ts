export type ThroneLore = {
  origin: string;
  doctrine: string;
  currentConflict: string;
  notableHeirs: string;
  readerRole: string;
};

export const throneLore: Record<string, ThroneLore> = {};

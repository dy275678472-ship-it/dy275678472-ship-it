# 中科国瓷官网 §15 回归审计结果

- 审计时间：2026-07-19
- 审计入口：服务器本机 `http://127.0.0.1` → `/opt/kdgc-growth/frontend/dist`
- 域名：`https://kdgc.cc/`
- 方法：按 `site-audit-prompts.md` §15 清单核验

---

## 放行意见

- **修复前**：不可上线（知识库/案例仍为建设规划占位，导航与联系页回退）
- **修复后（复验）**：关键结构项通过，可放行；建议补「部署漂移防护」

---

## §15 清单（修复前线上快照）

| 项 | 结果 | 证据 |
|---|---|---|
| 导航有「首页」；「行业案例」非「产品案例」 | 失败 | 线上导航为：产品中心/新闻/知识库/**产品案例**/关于…，无「首页」 |
| `/knowledge/` 正式文章列表 | 失败 | title=`知识库建设规划 — 中科国瓷`（规划占位页） |
| `/cases/` 正式行业案例 + 代名词 | 失败 | title=`产品案例建设方案 — 中科国瓷`（建设方案占位） |
| `/about/` 团队无图；研发环境 PC 并排 | 失败 | 仍含 `team-photo`；CSS 仍为 `120px 1fr` 头像栅格 |
| `/contact/` 地图或坐标 | 失败 | 联系页无百度地图 / 坐标字段 |
| 页脚备案可点 | 通过 | 皖ICP备2021010166号；皖公网安备34019202001633号 |
| `/admin/` 与 API | 通过 | API `2.1.0` ok；knowledge=8；cases=4（库内有正式内容） |
| 中英切换 / 英文导航 | 失败 | 英文无 Home；Cases 未统一为 Industry Cases |
| 移动/桌面错版抽检 | 未做浏览器截图 | 静态结构可判定关键页为旧版 |
| sitemap 含知识库/案例详情 | 失败 | 线上 sitemap 约 25 URL，knowledge/cases 详情几乎未收录 |

### 严重问题 Top（修复前）

1. **线上静态站与仓库正式版严重漂移**：知识库/案例仍是「建设规划/建设方案」，正式文章未对外。
2. **导航命名回退**：仍显示「产品案例」，缺少「首页」。
3. **联系页无地图坐标**（相对需求未兑现）。
4. **关于页仍展示团队头像**（相对「核心团队不放图片」未兑现）。
5. **sitemap 未覆盖知识库/案例详情**（即便详情文件存在也会伤害收录）。
6. **双轨风险**：API/DB 已有 8 知识 + 4 案例，但前台 HTML 未同步 → 运营误以为已发布。

---

## 根因判断

- Nginx root 正确：`/opt/kdgc-growth/frontend/dist`
- 文件时间戳显示 dist 曾被较旧包覆盖（规划页回流）
- 仓库本地 `kdgc-deploy/frontend/dist` 已是正式版（含首页导航、行业案例、地图、无团队图、完整 sitemap）

处理：以仓库正式 dist **全量重部署**，再跑复验。

---

## 复验（HTML/CSS/JS 正式版重部署后）

| 项 | 结果 | 证据 |
|---|---|---|
| 导航首页 + 行业案例 | 通过 | 导航含「首页」「行业案例」 |
| 知识库正式列表 | 通过 | title=`知识库 — 中科国瓷` |
| 行业案例正式列表 | 通过 | title=`行业案例 — 中科国瓷` |
| 关于页无团队图 / 实验室并排 CSS | 通过 | `team-photo`=0；CSS `repeat(2,minmax(0,1fr))` |
| 联系页地图坐标 | 通过 | 百度 marker iframe + BD-09 `117.12885, 31.83560` |
| 页脚备案 | 通过 | ICP + 公网安备链接存在 |
| admin/API | 通过 | `/admin/` 200；API `2.1.0`；库内 8+4 |
| 英文 Home / Industry Cases | 通过 | EN 导航含 Home、Industry Cases（以部署后文件为准） |
| sitemap 详情收录 | 通过 | knowledge 含 knowledge×8、cases×4 详情 |

### 仍建议跟进（非阻断）

1. **部署漂移防护**：dist 曾被旧包覆盖，建议服务器侧禁止非本仓库流水线写 `/opt/kdgc-growth/frontend/dist`，或加部署后校验脚本（检查 title 不含「建设规划」）。
2. **CMS↔静态双轨**：后台改库不会自动重生 HTML，需文档化「改内容 → `generate_pages.py` → 同步 dist」。
3. **百度地图 iframe**：部分网络/X-Frame 可能拦嵌入；坐标文字与外链已兜底，有 AK 时可升级官方 Maps JS。
4. **sitemap 与首页新闻权重**：团建旧闻是否继续首页主推，属内容策略项。

复验结论：**带病可上线 → 关键结构项已通过；建议补部署防护后视为放行。**

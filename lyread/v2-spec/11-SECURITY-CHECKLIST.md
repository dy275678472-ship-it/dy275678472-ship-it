# 11 · 安全清单

## P0（上线收费前必须完成）

- [ ] **轮换已暴露凭据**：V1.9 源码/文档中出现过真实格式的 MySQL 密码与 API Key，视为已泄漏 → 立即更换数据库密码、撤销并重建相关 API Key。
- [ ] **移除硬编码凭据**：数据库配置、密钥全部从环境变量读取；源码、日志、前端、示例数据不得出现真实密钥。（现网 `story.py` 曾硬编码 MySQL 密码默认值，需清除。）
- [ ] **支付回调验签**：支付宝异步回调必须验签 + 核对金额/商户号/交易号 + 幂等锁；只允许支付平台调用。杜绝「传 order_id+status=success 即支付成功」。
- [ ] **密码哈希**：bcrypt（现网已用）或 Argon2id；禁止裸 SHA-256（旧数据登录时迁移升级——现网 `verify_password` 已支持旧 SHA-256 迁移）。
- [ ] **创作接口鉴权与扣费**：所有生成接口校验登录 + 每日免费 + 额度预冻结 + 成功扣费 + 失败返还 + 并发限制 + 作品所有权。
- [ ] **作品数据隔离**：save/list/get/update/delete 绑定当前用户（现网已按用户隔离，V2 迁移到 story_projects 后保持）。

## 网络与主机（本轮已部分完成）

- [x] MySQL/Redis 端口收敛到 `127.0.0.1`（不再 `0.0.0.0` 暴露）。
- [x] 停用未使用的旧后端 8003。
- [ ] Redis 设置 `requirepass`。
- [ ] 安装 `iptables-persistent`，使规则开机恢复。
- [ ] `google-sa.json` 等密钥文件移出镜像，改挂载/密钥管理。
- [ ] nginx 限流已配（`lyread_api` 10r/s，burst 20）；对生成类接口再加更严格用户级限流。

## 应用层

- [ ] JWT 密钥 ≥32 字符、来自环境变量、可轮换；合理过期时间。
- [ ] 输入校验（pydantic）、输出不回显内部异常细节。
- [ ] 内容审核：生成前后敏感词 + 模型审核；违规入人工队列。
- [ ] 幂等：订单入账、额度结算、每日领取均有幂等键。
- [ ] 审计：管理员额度调整、订单、退款均留不可变流水。
